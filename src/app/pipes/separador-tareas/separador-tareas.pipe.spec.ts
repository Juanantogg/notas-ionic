import { SeparadorTareasPipe } from './separador-tareas.pipe';

describe('SeparadorTareasPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparadorTareasPipe();
    expect(pipe).toBeTruthy();
  });
});
